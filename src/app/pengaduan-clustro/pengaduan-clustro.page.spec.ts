import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PengaduanClustroPage } from './pengaduan-clustro.page';

describe('PengaduanClustroPage', () => {
  let component: PengaduanClustroPage;
  let fixture: ComponentFixture<PengaduanClustroPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PengaduanClustroPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
